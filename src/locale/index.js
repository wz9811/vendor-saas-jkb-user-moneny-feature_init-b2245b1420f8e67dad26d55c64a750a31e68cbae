import { i18n } from '@alife/ascp-base';

import defaultLocale from './config';

const { createLocale } = i18n;

export default createLocale('@ali/ascp-comp-vendor-saas-jkb-user-moneny', defaultLocale);
